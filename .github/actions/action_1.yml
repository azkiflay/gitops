# action.yml
name: "Hello World Action"
description: "Prints a hello message"
inputs:
  who-to-greet:
    description: "Who to greet"
    required: true
    default: "World"
runs:
  using: "node16"
  main: "index.js"
